"use strict";

import React from "react";
import { NativeModules, requireNativeComponent } from "react-native";
import useAbstractLayer from "../hooks/useAbstractLayer.js";
import { jsx as _jsx } from "react/jsx-runtime";
const MapLibreGL = NativeModules.MLNModule;
export const NATIVE_MODULE_NAME = "RCTMLNCircleLayer";
const RCTMLNCircleLayer = requireNativeComponent(NATIVE_MODULE_NAME);

/**
 * CircleLayer is a style layer that renders one or more filled circles on the map.
 */
const CircleLayer = ({
  sourceID = MapLibreGL.StyleSource.DefaultSourceID,
  ...props
}) => {
  const {
    baseProps,
    setNativeLayer
  } = useAbstractLayer({
    ...props,
    sourceID
  });
  return /*#__PURE__*/_jsx(RCTMLNCircleLayer, {
    testID: "rctmlnCircleLayer",
    ref: setNativeLayer,
    ...baseProps
  });
};
export default CircleLayer;
//# sourceMappingURL=CircleLayer.js.map